@{
    Layout = "_Layout";
}

@using BestRestaurants.Models;

<h1>Restaurants</h1>

@if (@Model.Count == 0)
{
  <h3>No restaurants have been added yet!</h3>
} 

<ul>
@foreach (Restaurant restaurant in Model)
{
  // Austin note: the syntax below is wrong, click on one and look at the url
  <li>@Html.ActionLink($"{restaurant.Name}", "Name of restaurant", new { id = restaurant.RestaurantId })</li>
  }
</ul>
